__pycache__/
*.pyc

# results/ 里的【文本结果】进仓库 —— 小（17 个文件 106 KB），而且是结论本身，
# 丢了就得重新上机器人测。当天是 2026-09-21：整晚的测量只存在板子上，
# 差点随 SD 卡一起没。
#
# 【二进制】不进 —— bag / 回放 / 视频 / 抽帧图，合计 3.1 MB，
# 且都能从文本结果或原始数据重新生成。
# ⚠️ 用 `results/`（整目录）会把 JSON 一起扫出去，而且是【静默】的：
#    `git add results/` 不报错、什么都不加。这正是本项目一直在防的那类失败。
results/**/*.db3
results/**/*.npz
results/**/*.mp4
results/**/*.png
